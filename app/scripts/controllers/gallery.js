'use strict';

/**
 * @ngdoc function
 * @name codeApp.controller:GalleryCtrl
 * @description
 * # GalleryCtrl
 * Controller of the codeApp
 */
function GalleryCtrl($scope, imageData, titleData, userData, projectData, ngDialog) {

};